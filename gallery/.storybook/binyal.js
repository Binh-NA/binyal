import { create } from '@storybook/theming';
import imgBinyal from '../assets/binyal-robo.png';

export default create({
  base: 'light',
  brandTitle: 'Binyal - storybook',
  brandUrl: '/',
  brandImage: imgBinyal,
});
